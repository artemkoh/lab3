word = input("Введіть слово: ")

# Перевірка, чи довжина рядка достатня для виконання операції зрізу
if len(word) >= 4:
    # Отримання зрізу від 2-го символа до 2-го з кінця (включаючи 2-й символ з кінця)
    sliced_word = word[1:-2+1]  # Оскільки індексація з нуля, для 2-го символа потрібно почати з 1
    print(f"Результат зрізу: {sliced_word}")
else:
    print("Рядок занадто короткий для виконання цього зрізу.")
